#!/bin/bash
# Скрипт для сборки Linux версии

set -e

echo "🔨 Сборка Linux версии Instagram Reels Publisher..."

# Проверяем наличие uv
if ! command -v uv &> /dev/null; then
    echo "❌ uv не установлен. Установите uv: curl -LsSf https://astral.sh/uv/install.sh | sh"
    exit 1
fi

# Синхронизируем зависимости (установит PyInstaller если нужно)
echo "📦 Синхронизация зависимостей..."
uv sync

# Очищаем предыдущие сборки
echo "🧹 Очистка предыдущих сборок..."
rm -rf build dist

# Собираем с помощью PyInstaller через uv
echo "📦 Сборка приложения..."
uv run pyinstaller build.spec \
    --clean \
    --noconfirm

# Копируем браузеры Playwright если нужно
echo "🌐 Проверка браузеров Playwright..."
PLAYWRIGHT_BROWSERS_PATH=$(uv run python -c "from playwright.sync_api import sync_playwright; p = sync_playwright().start(); print(p.chromium.executable_path)" 2>/dev/null || echo "")

if [ -n "$PLAYWRIGHT_BROWSERS_PATH" ]; then
    echo "✅ Браузеры Playwright найдены"
    # Браузеры будут загружены автоматически при первом запуске
else
    echo "⚠️ Браузеры Playwright не найдены. Они будут загружены при первом запуске."
fi

echo ""
echo "✅ Сборка завершена!"
echo "📁 Исполняемый файл находится в: dist/instagram-reels-publisher"
echo ""
echo "💡 Примечание: При первом запуске Camoufox/Playwright могут загрузить браузеры."
echo "   Это нормально и произойдет автоматически."
