# Instagram Reels Publisher

Автоматизация публикации Reels в Instagram с графическим интерфейсом.

## Возможности

- 📤 **Автопостинг Reels** - автоматическая публикация видео с описанием
- 🔐 **Открыть Instagram** - открытие браузера с сохранением сессии и автообновлением куков
- 🕵️ **Антидетект браузер** - открытие чистого браузера без куков

## Установка

```bash
# Клонируй репозиторий
git clone <repo-url>
cd browser-automation

# Установи зависимости
uv sync
# или
pip install -e .
```

## Использование

```bash
# Запуск GUI
uv run python -m browser_automation.main
# или
python -m browser_automation.main
```

## Сборка исполняемого файла

### Важно: Сборка для текущей платформы

PyInstaller собирает приложение **только для текущей платформы**:
- На **Linux** → собирается Linux версия → `dist/instagram-reels-publisher`
- На **Windows** → собирается Windows версия → `dist/instagram-reels-publisher.exe`

### Linux

```bash
chmod +x build.sh
./build.sh
```

Исполняемый файл будет в `dist/instagram-reels-publisher`

### Windows

```cmd
build.bat
```

Исполняемый файл будет в `dist\instagram-reels-publisher.exe`

### Сборка для другой платформы

См. [PLATFORMS.md](PLATFORMS.md) для инструкций по сборке для другой платформы.

### Подробные инструкции

См. [BUILD.md](BUILD.md) для детальной информации о сборке.

## Важные замечания

1. **Браузеры**: При первом запуске Camoufox/Playwright автоматически загрузят браузеры. Это нормально.

2. **Сессии**: Сессии сохраняются в `cache/sessions.json` и автоматически загружаются при следующем запуске.

3. **Multiprocessing**: Приложение использует отдельные процессы для каждого браузера, что обеспечивает стабильность.

4. **Размер сборки**: Из-за включения PySide6 и Playwright, размер может быть большим (100-200 МБ).

## Структура проекта

```
browser-automation/
├── src/
│   └── browser_automation/
│       ├── automation.py          # Основная логика автоматизации
│       ├── automation_worker.py   # Worker для multiprocessing
│       ├── gui_modern.py          # GUI интерфейс
│       └── main.py                # Точка входа
├── build.spec                     # Конфигурация PyInstaller
├── build.sh                        # Скрипт сборки для Linux
├── build.bat                       # Скрипт сборки для Windows
└── BUILD.md                       # Детальные инструкции по сборке
```

## Требования

- Python 3.14+
- PySide6
- Camoufox
- Playwright

## Лицензия

MIT
