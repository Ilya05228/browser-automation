# Browser Automation

GUI для управления профилями Camoufox с поддержкой VLESS-прокси.

## Возможности

- **Таблица профилей** — название, ID, статус (запущен / не запущен)
- **Множественный выбор** — выделите один или несколько профилей (Ctrl/Shift + клик)
- **Действия с выделенным:**
  - ➕ **Создать** — новый профиль
  - ✏️ **Изменить** — редактирование (двойной клик по строке)
  - 📋 **Дублировать** — копия выделенных
  - 📤 **Экспорт в буфер** — JSON в буфер обмена
  - 📤 **Экспорт в файл** — сохранение в `.json`
  - 📥 **Импорт из файла** / **из буфера**
  - 🗑️ **Удалить**
  - 🚀 **Запуск** — Camoufox с профилем (VLESS поднимается автоматически)
  - ⏹️ **Завершить** — закрыть браузер и xray

Окно браузера получает заголовок = название профиля. Статус обновляется при ручном закрытии браузера.

## Запуск

```bash
uv run main
# или
uv run browser-automation
```

## Зависимости

- **Camoufox** — антидетект браузер (ставится через `uv sync`)
- **Xray-core** — для VLESS-прокси. Нужен в PATH:
  - **Windows:** `xray.exe` — [скачать](https://github.com/XTLS/Xray-core/releases)
  - **Ubuntu/Linux:** `apt install xray` или [официальный скрипт](https://github.com/XTLS/Xray-install)

**Установка Xray:**
- [Загрузка и установка Xray](https://xtls.github.io/ru/document/install.html)
- [Релизы Xray-core](https://github.com/XTLS/Xray-core/releases)

## Профили

Хранятся в `~/.config/browser-automation/profiles.json`. Каждый профиль: название, VLESS-строка (опционально), настройки прокси.
